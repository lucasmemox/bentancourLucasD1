import { EstiloBoleanoDirective } from './estilo-boleano.directive';

describe('EstiloBoleanoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloBoleanoDirective();
    expect(directive).toBeTruthy();
  });
});
