import { CabeceraTextoDirective } from './cabecera-texto.directive';

describe('CabeceraTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new CabeceraTextoDirective();
    expect(directive).toBeTruthy();
  });
});
