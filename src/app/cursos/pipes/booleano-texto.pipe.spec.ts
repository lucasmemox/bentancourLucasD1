import { BooleanoTextoPipe } from './booleano-texto.pipe';

describe('BooleanoTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanoTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
